 import { FFprobeKit } from 'ffmpeg-kit-react-native';

export const getAudioSampleRate = async () => {
  //   try {
  //     const information = await FFprobeKit.getMediaInformation(
  //       `${audioFilePath}`
  //     );
  //     const res = information.getMediaInformation();
  //     console.log(res.getStreams()[0].getSampleRate());
  //     return res.getStreams()[0].getSampleRate();
  //   } catch (error) {
  //     console.error('Error retrieving media information:', error);
  //     return null;
  //   }
};
